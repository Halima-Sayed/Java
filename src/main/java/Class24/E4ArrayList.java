package Class24;


import java.util.ArrayList;

public class E4ArrayList {
    public static void main(String[] args) {
        //Integer integer=new Integer(10); <- no need to use this convert the primitives to a wrapper type this is called boxing
        Integer integer1=10; //convert the primitives to a wrapper type this shorter way is called autoboxing
        int num=integer1.intValue(); //<- no need to use this unboxing
        int num2=integer1; // shorter way auto unboxing

        Integer integer2=10;

        int number=integer2;
    }
}
